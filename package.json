{
  "name": "basic-pong",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite --host 0.0.0.0 --port 3000",
    "preview": "vite preview --host 0.0.0.0 --port 3000",
    "start": "npm run dev"
  },
  "devDependencies": {
    "vite": "^8.1.4"
  }
}
